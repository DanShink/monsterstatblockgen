import React from "react";
import { useMonsterStatblockContext } from "../MonsterStatblockContext";

export default function ImportAndExport() {
	const {
		name,
		size,
		type,
		level,
		category,
		monsterStatus,
		hp,
		mig,
		agl,
		cha,
		int,
		cm,
		prime,
		migprof,
		aglprof,
		chaprof,
		intprof,
		ad,
		adBonus,
		pd,
		pdBonus,
		pdr,
		edr,
		mdr,
		resistances,
		vulnerabilities,
		immunities,
		conditionImmunities,
		conditionResistances,
		conditionVulnerabilities,
		skills,
		senses,
		languages,
		speeds,
		features,
		ap,
		legendaryAp,
		defaultSpeed,
		check,
		saveDC,
		actions,
		reactions,
	} = useMonsterStatblockContext();
	return <div>Import/Export Statblocks</div>;
}
